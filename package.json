{
  "name": "build-app",
  "version": "0.0.1",
  "description": "Build full-stack JS app",
  "main": "index.js",
  "scripts": {
    "build": "cd server && npm run build && cd .. && node ./tasks/build.js",
    "build:test": "npm run build && node ./build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "fs-extra": "^1.0.0"
  },
  "author": "Yegor Sytnyk",
  "license": "ISC"
}
